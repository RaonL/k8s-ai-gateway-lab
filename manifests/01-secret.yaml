# This template is rendered by scripts/deploy.sh. Do not apply it directly:
# it deliberately contains an environment-variable placeholder instead of a key.
apiVersion: v1
kind: Secret
metadata:
  name: nvidia-secret
  namespace: ai-gateway
type: Opaque
stringData:
  api-key: "${NVIDIA_API_KEY}"
