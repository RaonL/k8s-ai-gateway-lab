apiVersion: v1
kind: Service
metadata:
  name: ai-proxy-svc
  namespace: ai-gateway
spec:
  type: ClusterIP
  selector:
    app: ai-proxy
  ports:
    - name: http
      port: 80
      targetPort: http
